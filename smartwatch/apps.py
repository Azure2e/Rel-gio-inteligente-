from django.apps import AppConfig


class SmartwatchConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'smartwatch'
    verbose_name = 'Relógio Inteligente'   # Nome que aparece no admin